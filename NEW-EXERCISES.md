# 次に追加する種目のメモ (2026-09-18 に教わった。まだ実装しない)

`core.js` の `DEFAULT_EXERCISES` に足すときのための下書き。
左右のペアや秒数の扱いなど、実装前に確認したい点は「?」で残してある。

---

## 1. 階段2段目に片足、もも伸ばし

階段の2段目に右足を置いて、左足のももを伸ばす。
このとき身体が左に開かないよう、まっすぐをキープ。

- 30秒
- ? 左右あり (右足を段に置く/左足を段に置く) と思われる。sideKnee などと同じ形
- ? 「まっすぐキープ」をイラストでどう見せるか (背骨が段の方向へ流れない、くらいの図になりそう)

## 2. 階段1段目に横向き、足の前後振り

階段の1段目に横向きに立ち、右足を段に置いて、左足を前後にブラブラ振る。
できるだけ前後同じ大きさで振る。

- 30秒
- ? 左右あり
- 動きとしては footRoll (転がす) より hipTwist の振り子に近い。左右へではなく前後へ振るので、
  真横から見た構図が良さそう

## 3. 浅いスクワット

肩幅に立ち、膝を少し曲げるだけの浅いスクワット。

- 30秒
- 左右なし

## 4. 股割りスクワット (支え有り)

何かに捕まって行う、股割り (脚を大きく開いた) スクワット。

- ? 秒数/回数の指定なし。既存にならって 20回、または他の項目にそろえて 30秒か
- 左右なし
- 「捕まる」動作 (椅子の背やてすりなど) をイラストにどう入れるか要検討

## 5. お尻を後ろに引くスクワット (支え有り)

何かに捕まって、お尻を後ろに引くように下げるスクワット。無理しない範囲で。

- ? 秒数/回数の指定なし
- 左右なし
- 「無理しない程度に」という注記を、文面に残すかどうか (他の種目には無い言い回し)

## 6. 四つん這い、対角に伸ばす (バードドッグ)

四つん這いになり、右手を前に出す。左手と右足は動かさずそのまま。
左足を後ろに伸ばす。

- 10秒
- ? 左右あり (右手+左足 / 左手+右足)。既存の中では一番秒数が短い (他は20回 or 30秒)
- 対角の手足を同時に伸ばす、支えは残り2本の手足という構図。四つん這い系は catPose があるので
  同じ floorBase が使えそう

## 7. 横向きで肩甲骨、足くの字ロック

右手を上にして横向きに寝る。足はくの字に曲げてロックさせる (安定させる)。
右肩だけを後ろに動かして肩甲骨を広げる。

- ? 秒数の指定なし
- ? 左右あり
- 既存の sideOpen (横向きに寝て膝を曲げ、上の手を反対側へ開く) とかなり近い動き。
  「足をくの字にロック」という下半身の固定のしかたが今の sideOpen の絵と合っているか、
  それとも別の種目として立てるべきか、次に聞く

---

### 実装するときに思い出すこと (CLAUDE.md より)

- 種目は `art` の名前で引く。並び順 (`ex0` など) では引かない
- 左右ペアは `sides: ['left', 'right']`、`sideNames` で文面を変える
- 秒数だけの種目 (今は無い) を足すなら、回数表示 (`reps` + 「回」) をどう扱うか core.js 側の設計を先に決める。
  今のところ全種目「〇〇回」表示で統一されているので、「30秒」を混ぜるなら表示の出し分けが要る
- イラストは実物の動きを見て (写真か、候補を複数出して選んでもらう) から描く。
  最初の一発描きは大体ずれる
